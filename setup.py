from setuptools import setup, find_packages

setup(
    name='yuurisan-lib',
    version='1.0.0',
    description='No comment',
    author='Yuurichan-N3',
    packages=find_packages(),
    install_requires=[
        'colorama',
        'pyfiglet'
    ],
)
